import os
import json

def prepare_base_folder(config):
    {
        # TODO preparing foldes

        # base_folder = config['base-folder']
        #
        # if not os.path.exists(base_folder):
        #     os.makedirs(base_folder)
        #
        # if not os.path.exists(os.path.join(config['base-folder'], 'classification')):
        #     os.makedirs(os.path.join(config['base-folder'], 'classification'))
        #
        # json.dump(config.config, open(os.path.join(base_folder, 'configuration.json'), 'w'), indent=4, sort_keys=True)

    }
